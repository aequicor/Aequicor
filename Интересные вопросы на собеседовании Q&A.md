
**Минусы LiveData**
- работают на main потоке, поэтому был придуман метод postValue
- из коробки нет SingleLiveEvent для обработки события один раз

**Модель памяти Kotlin**

В Java память разделена на две области Stack и Heap. В стеке хранятся примитивные типы и ссылки на объекты в рамках локальной функции. В хипе живут все объекты, массивы. Управляется через GC.

